# Prometheus

### Install with:

Install prometheus
```
cd deployments/prometheus
./install.sh
```
#### Passwords

Please note, root password is generated randomly with each install

### Uninstall with:

```
cd deployments/prometheus
./uninstall.sh
```
